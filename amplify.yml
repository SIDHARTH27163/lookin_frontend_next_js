version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build-all
  artifacts:
   baseDirectory: ./dist/apps/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
